# Xtract Audio Extraction Service - Environment Variables
# Copy this file to .env and fill in your values

# Node environment
NODE_ENV=development

# Server port
PORT=8080

# CORS allowed origins (comma-separated, or * for all)
ALLOWED_ORIGINS=*

# Temporary file storage directory
TEMP_DIR=/tmp/xtract-processing

# Maximum video file size in MB (default: 300)
MAX_FILE_SIZE_MB=300

# Cleanup interval in minutes (default: 5)
CLEANUP_INTERVAL_MINUTES=5

# Supabase configuration (REQUIRED)
# Use SERVICE ROLE key for backend operations (bypasses RLS)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# FFmpeg paths (optional - uses system ffmpeg if not set)
# FFMPEG_PATH=/usr/bin/ffmpeg
# FFPROBE_PATH=/usr/bin/ffprobe
